<%- include partials/header -%>
    <h1>Home</h1>
    <p>
        <%= hs %>
    </p>
    <% postss.forEach(function(x){ %>
        <h1>
            <%=x.title%>
        </h1>
        <p>
            <%= x.body.substring(0,100)+"..." %>
                <% var y=x.title%>
                    <a href="/posts/<%= y %> ">Read more</a>
        </p>

        <%}) %>
            <%- include partials/footer -%>